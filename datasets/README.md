# Datasets

This repository tracks lightweight dataset metadata under `datasets/metadata/`.

Raw packet captures are intentionally kept out of git by `.gitignore` because they
can be large, sensitive, or governed by third-party redistribution terms. Place
local PCAP files under `datasets/raw/` when running the parser scripts.
