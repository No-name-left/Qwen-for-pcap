# Git commit summary: Zeek large-scale test

## Included

- Zeek parser fixes and parser_source/evidence annotations.
- Prompt/RAG targeted patch.
- Toolchain, Zeek parse, Suricata parse, rebuild, coverage, eval, API, patch, and SFT metadata reports.
- Small eval/SFT metadata files and CSV manifests.

## Excluded

- `.env`, token values, PCAP/cap/pcapng, binetflow, large flow CSVs, parsed logs, prompt full directories, raw/parsed API responses, model weights, and adapters.
